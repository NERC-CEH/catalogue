# MCP server configuration — activated by the "mcp-server" profile.
# Exposes catalogue search tools to external LLMs via SSE transport:
#   GET  /mcp/sse      — event stream
#   POST /mcp/messages — client-to-server messages
spring.ai.mcp.server.name=ukceh-catalogue
spring.ai.mcp.server.version=1.0.0
spring.ai.mcp.server.type=SYNC
