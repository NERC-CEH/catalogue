# Enable Amazon Bedrock Titan Embed Text v2 for vector search.
# Add "vector-search" to SPRING_PROFILES_ACTIVE to activate.
# Requires AWS credentials in the environment (AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY or IAM role).
spring.ai.bedrock.titan.embedding.enabled=true
