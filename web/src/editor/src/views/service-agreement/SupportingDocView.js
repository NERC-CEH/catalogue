import { ObjectInputView } from '../index'
import template from '../../templates/service-agreement/SupportingDoc.tpl'
import _ from 'underscore'

export default ObjectInputView.extend({ template: _.template(template) })
