<#list doc.distributionFormats>
  <formats>
    <#items as format>
      <#if format.type?has_content>
        <format>${format.type} ${format.name}</format>
      <#else>
        <format>${format.name}</format>
      </#if>
    </#items>
  </formats>
</#list>
